#pragma once
#include <functional>

namespace Rocket {
    // In-place Sort
    template<typename T>
    void SortBF(T* data, std::size_t size, std::function<bool(T&, T&)> comapre) {

    }

    // In-place Sort
    template<typename T>
    void SortSF(T* data, std::size_t size, std::function<bool(T&, T&)> comapre) {

    }
}
