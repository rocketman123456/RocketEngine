#pragma once
#include <functional>

namespace Rocket {
    template<typename T>
    void SortBigFirst(T* data, std::size_t size, std::function<bool(T&, T&)>) {

    }

    template<typename T>
    void SortLittleFirst(T* data, std::size_t size, std::function<bool(T&, T&)>) {

    }
}
