#pragma once
#include <functional>

namespace Rocket {
    // In-place Sort
    template<typename T>
    void SortBigFirst(T* data, std::size_t size, std::function<bool(T&, T&)> comapre) {

    }

    // In-place Sort
    template<typename T>
    void SortLittleFirst(T* data, std::size_t size, std::function<bool(T&, T&)> comapre) {

    }
}
