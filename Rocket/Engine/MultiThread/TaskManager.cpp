#include "MultiThread/TaskManager.h"

namespace Rocket {
    int32_t TaskManager::Initialize() {
        return 0;
    }

    void TaskManager::Finalize() {
    }

    void TaskManager::Tick(TimeStep step) {
    }
}
